repos:
  - repo: local
    hooks:
      - id: lint-suite
        name: lint suite (ruff check + format)
        entry: pwsh ./tools/linting/run_lint_suite.ps1 -Fix
        language: system
        types: [python]
        pass_filenames: true
