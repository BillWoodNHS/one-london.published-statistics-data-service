select
    EVENT_TIMESTAMP_UTC,
    RUN_ID,
    CONTRACT_VERSION,
    STAGE,
    STATUS,
    ATTEMPT_NUMBER,
    SERIES_ID,
    SUB_DATASET_ID,
    SOURCE_URL,
    FILE_NAME,
    SOURCE_CONTENT_HASH,
    LOAD_ID,
    SOURCE_BYTES,
    RAW_ROW_COUNT,
    NORMALIZED_ROW_COUNT,
    NORMALIZED_BYTES,
    UPLOADED_PATH,
    SKIP_REASON,
    ACQUISITION_METHOD,
    DURATION_MS,
    ERROR_TYPE,
    ERROR_MESSAGE,
    DISCOVERED_FILE_COUNT
from {{ source('ingest', 'INGEST_FUNCTION_APP_EVENTS') }}
